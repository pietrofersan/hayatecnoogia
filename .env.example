# ── Supabase ────────────────────────────────────────────────────────
NEXT_PUBLIC_SUPABASE_URL=https://ghkckfamnpivlwlcjoez.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=          # só servidor: webhooks, leads, crons, import

# ── Asaas (F1 roda 100% no sandbox) ─────────────────────────────────
ASAAS_BASE_URL=https://api-sandbox.asaas.com/v3
ASAAS_API_KEY=
ASAAS_WEBHOOK_TOKEN=                # mesmo valor cadastrado no painel do Asaas

# ── ZapSign ─────────────────────────────────────────────────────────
ZAPSIGN_BASE_URL=https://sandbox.api.zapsign.com.br/api/v1
ZAPSIGN_TOKEN=
ZAPSIGN_WEBHOOK_TOKEN=              # vai na URL do webhook como ?t=

# ── Geração de PDF do contrato ──────────────────────────────────────
GOTENBERG_URL=                      # ex.: https://gotenberg.suainstancia.com

# ── Avisos internos ─────────────────────────────────────────────────
LEAD_NOTIFY_EMAIL=pietro@fersan.co  # aceita lista separada por vírgula
RESEND_API_KEY=                     # sem chave, os avisos só vão para o log
NOTIFY_FROM=master@hayatecnologia.com.br

# ── App ─────────────────────────────────────────────────────────────
NEXT_PUBLIC_APP_URL=http://localhost:3000
CRON_SECRET=                        # Authorization: Bearer <valor> nos crons

# ── Módulo 1 · Inteligência de mercado ───────────────────────────────
GEMINI_API_KEY=                     # expansão de segmento em palavras (Gemini Flash)
                                     # grátis em aistudio.google.com, sem cartão
                                     # sem chave, o botão "Expandir com IA" retorna erro
